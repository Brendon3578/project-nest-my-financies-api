import { InvalidRelationExceptionFilter } from './invalid-relation-exception.filter';

describe('InvalidRelationExceptionFilter', () => {
  it('should be defined', () => {
    expect(new InvalidRelationExceptionFilter()).toBeDefined();
  });
});
